const {aboutPageHandle, contactPageHandle, singlePostHandle} = require("./pages");
const notFoundHandle = require("./errors");

module.exports = {
    aboutPageHandle,
    contactPageHandle,
    singlePostHandle,
    notFoundHandle,
}